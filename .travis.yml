language: csharp
mono: none
dist: bionic
dotnet: 2.2

services:
  - docker

install:
  - source $TRAVIS_BUILD_DIR/.travis/stage_install.sh
  - dotnet restore
  

script:
  - dotnet build
  - dotnet test Master40.XUnitTest

notifications:
  slack: infocomhtw:7H5jePcDELUv916neaRS3LQ1
