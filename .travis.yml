language: csharp
mono: none
dist: bionic
dotnet: 2.2

services:
  - docker

install:
  - source $TRAVIS_BUILD_DIR/.travis/stage_install.sh
  - dotnet restore
  

script:
  - dotnet build

notifications:
  email:
- krockert@htw-dresden.de