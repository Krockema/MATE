using NSimAgentTest.Agents.Internal;
using NSimulate;

namespace NSimAgentTest.Agents
{
    public class SystemAgent : Agent
    {
        public SystemAgent(Agent creator, string name, bool debug) : base(creator, name, debug)
        {

        }
    }
}