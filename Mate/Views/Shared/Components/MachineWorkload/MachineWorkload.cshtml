@{
    ChartJSCore.Models.Chart chart = (ChartJSCore.Models.Chart)ViewData[index: "chart"];
    string raw = "Chart Rendering Failed";
    string machine = ViewData[index: "Machine"].ToString().Replace(oldValue: " ", newValue: "");
    if (chart != null) {
        raw = chart.CreateChartCode(canvasId: machine);
    }
}

<style>
    .chartWrapper {
        position: relative;
            
    }

    .chartWrapper > canvas {
        position: absolute;
        left: 0;
        top: 0;
        pointer-events:none;
    }
    .chartAreaWrapper {
        position: relative;
        width: 100%;
    }

    .chartAreaWrapper2 {
          
        position: relative;
        height: 100px;
    }
</style>
<h4>@ViewData[index: "Machine"] Workload</h4>
<div class="chartWrapper">
    <div class="chartAreaWrapper">
        <div class="chartAreaWrapper2">
                <!--div id="js-legend" class="chart-legend"><div-->
            <canvas id="@machine" height="100" width="100"></canvas>
            </div>
    </div>
</div>

<script>
    @Html.Raw(value: raw);
</script>
