@using Master40.DB.Models
@model IEnumerable<Master40.DB.Models.ProductionOrderBom>

@{
    string level = ViewData["level"]?.ToString() ?? "";
    level = (level != "") ? level + "." : level;
    int count = 0;
    string display = "";
}
@if (Model.Count() > 0 && Model != null)
{
    foreach (var item in Model)
    {
        <tr>
            <td>
                @{ count = count + 1; display = level + count.ToString(); }
                @display
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Quantity)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ProductionOrderParent.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ProductionOrderParent.Article.Name)
            </td>
            <td>

            </td>

        </tr>
      /*  if (item.ProductionOrderBomChild.ProductionOrderBoms != null)
        {
            ViewData["level"] = level + count.ToString();
        @Html.Partial("ProductionOrderBom", item.ProductionOrderBomChild.ProductionOrderBoms, this.ViewData)
        }
        */
    }
}
