@model List<Master40.Models.LogMessage>

@{
    ViewBag.Title = "title";
    Layout = "_Layout";
    int LineNumber = 0;
}
<script>
    function submitform() {
        $('form').submit();
    }
</script>


@await Component.InvokeAsync("ProductionTimeline")



<div class="row">
    <div class="col-md-8">
        <h2>MRP </h2>
        <p>
            Container to Process the MRP Run and Show the Results.
        </p>
        <p><a class="btn btn-default" href="~/Mrp/MrpProcessing">run the whole MRP process</a></p>
        <div>
            <P>Iterate through each step</P>
            <p><a class="btn btn-default" href="~/Mrp/MrpBackward">Schedule backward</a>
            </p>
            <p><a class="btn btn-default" href="~/Mrp/MrpForward">Schedule forward</a>
            </p>
            <p><a class="btn btn-default" href="~/Mrp/CapacityPlanning">Plan capacities</a>
            </p>
            <p><a class="btn btn-default" href="~/Mrp/MrpGifflerThompson">Giffler-Thompson</a>
            </p>
        </div>
    </div>
    <div class="col-md-4">
        <h2>Result</h2>
        @foreach (var item in Model)
        {
            <div class="alert alert-@item.MessageType.ToString()">
                @{ LineNumber = LineNumber + 1; }
                <strong> @LineNumber.ToString() :</strong> @Html.DisplayFor(modelItem => item.Message)
            </div>
                    }
    </div>
</div>
