@model IEnumerable<Master40.DB.DataModel.T_ProductionOrder>

@{
    ViewData[index: "Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
                <th>
                    @Html.DisplayNameFor(expression: model => model.Quantity)
                </th>
                <th>
                    @Html.DisplayNameFor(expression: model => model.Name)
                </th>
            <th>
                @Html.DisplayNameFor(expression: model => model.Article)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(expression: modelItem => item.Quantity)
            </td>
            <td>
                @Html.DisplayFor(expression: modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(expression: modelItem => item.Article.Id)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
