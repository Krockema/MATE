@model Master40.DB.DataModel.M_ArticleBom

@{
    ViewData["Title"] = "Details";
    Layout = "";
}

<h2>Details</h2>

<div>
    <h4>ArticleBom</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Quantity)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.Quantity)
        </dd>
        <dt>
            @Html.DisplayNameFor(model => model.Name)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.Name)
        </dd>
        <dt>
            @Html.DisplayNameFor(model => model.ArticleChild)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.ArticleChild.Name)
        </dd>
        <dt>
            @Html.DisplayNameFor(model => model.ArticleParent)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.ArticleParent.Name)
        </dd>
    </dl>
</div>

@if (Model.ArticleChild.PictureUrl != null)
{
    <div>
        <img src="@Html.Raw(Model.ArticleChild.PictureUrl)" width="550"/>
    </div>
}

<div class="modal-footer">
    <button type="button" class="btn btn-default" data-dismiss="modal">Back</button>
    <a asp-action="Edit" class="btn-default btn" asp-route-id="@Model.Id"><span class="glyphicon glyphicon-edit" aria-hidden="true"></span> Edit</a>
</div>
