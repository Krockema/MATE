@model Master40.DB.DataModel.Order

@{
    ViewData["Title"] = "Details";
    string modelId = Model.Id.ToString();
}

<h3>Order - Details</h3>

<div>
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Name)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.Name)
        </dd>
        <dt>
            @Html.DisplayNameFor(model => model.DueTime)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.DueTime)
        </dd>
        <dt>
            @Html.DisplayNameFor(model => model.BusinessPartner)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.BusinessPartner.Id)
        </dd>
    </dl>
</div>
@Html.Partial(@"..\OrderParts\Index.cshtml", @Model.OrderParts)
<div class="modal-footer">
    <button type="button" class="btn btn-default" data-dismiss="modal">Back</button>
    <a asp-action="Create" asp-controller="OrderParts" data-target="#modal-sub-container" asp-route-orderId="@modelId" data-toggle="modal" class="btn btn-default">
    <span class="glyphicon glyphicon-edit" aria-hidden="true"></span> Add Part </a>
</div>